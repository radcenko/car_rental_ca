<nav id="navbar" class="navbar navbar-expand-lg navbar-dark bg-dark position-fixed w-100 fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/vehicles">Vehicle Rentals</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/vehicles">Vehicles</a>
                </li>
                <li class="nav-item">
                    <% if (user && user.RoleId === 1) { %>
                        <a class="nav-link" href="/colours">Colours</a>
                    <% } else { %>
                        <span class="nav-link disabled" tabindex="-1" aria-disabled="true">Colours</span>
                    <% } %> 
                </li>
                <li class="nav-item">
                    <% if (user && user.RoleId === 1) { %>
                        <a class="nav-link" href="/types">Vehicle Types</a>
                    <% } else { %>
                        <span class="nav-link disabled" tabindex="-1" aria-disabled="true">Vehicle Types</span>
                    <% } %>                    
                </li>
                <% if (!user) { %>
                    <li class="nav-item">
                        <span class="nav-link"><i class="bi bi-people"></i> Guest</span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Sign in</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <p class="nav-link"><i class="bi bi-people-fill"></i> <%= user.FullName %></p>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>
                <% }; %>
            </ul>
        </div>
    </div>
</nav>